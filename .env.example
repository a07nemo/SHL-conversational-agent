# Copy this to .env for local dev (or set as env vars on your hosting platform).

# Required: get a free key at https://console.groq.com/keys
GROQ_API_KEY=your_key_here

# Optional: override the model (default shown below)
LLM_MODEL=llama-3.3-70b-versatile
